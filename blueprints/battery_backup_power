blueprint:
  name: Batterie-Backup dynamisch mit Prognose
  description: Dynamische Anpassung des Batterie-Backup-Werts basierend auf Energieprognose
  domain: automation
  input:
    entity_today:
      name: Sensor für heutige Prognose
      description: Sensor, der die Energieprognose für heute liefert
      selector:
        entity:
          domain: sensor
    entity_tomorrow:
      name: Sensor für morgige Prognose
      description: Sensor, der die Energieprognose für morgen liefert
      selector:
        entity:
          domain: sensor
    entity_d2:
      name: Sensor für Übermorgen
      description: Sensor, der die Energieprognose für übermorgen liefert
      selector:
        entity:
          domain: sensor
    entity_d3:
      name: Sensor für in 3 Tagen
      description: Sensor, der die Energieprognose für in drei Tagen liefert
      selector:
        entity:
          domain: sensor
    backup_target:
      name: Backup Power Ziel-Entität
      description: Entität zur Einstellung des Batterie-Backup-Werts
      selector:
        entity:
          domain: number
    notify_service:
      name: Benachrichtigungsdienst
      description: Dienst für die Benachrichtigung (z. B. mobile App)
      selector:
        text:
    min_backup:
      name: Minimaler Backup-Wert (%)
      description: Untergrenze für den Batterie-Backup-Wert in Prozent
      default: 12
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
    max_backup:
      name: Maximaler Backup-Wert (%)
      description: Obergrenze für den Batterie-Backup-Wert in Prozent
      default: 30
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
    min_forecast:
      name: Minimaler Prognosewert (kWh)
      description: Untergrenze für die Energieprognose in kWh
      default: 5
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "kWh"
    max_forecast:
      name: Maximaler Prognosewert (kWh)
      description: Obergrenze für die Energieprognose in kWh
      default: 25
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "kWh"
    cutoff_hour:
      name: Uhrzeit-Schwelle (Stunde)
      description: Stunde des Tages, ab der die heutige Prognose ignoriert wird
      default: 12
      selector:
        number:
          min: 0
          max: 23
          unit_of_measurement: "h"

trigger:
  - platform: state
    entity_id:
      - !input entity_today
      - !input entity_tomorrow
      - !input entity_d2
      - !input entity_d3

condition: []

action:
  - variables:
      now_hour: "{{ now().hour }}"
      forecast_today: "{{ states(!input entity_today) | float(0) }}"
      forecast_tomorrow: "{{ states(!input entity_tomorrow) | float(0) }}"
      forecast_d2: "{{ states(!input entity_d2) | float(0) }}"
      forecast_d3: "{{ states(!input entity_d3) | float(0) }}"
      min_f: !input min_forecast
      max_f: !input max_forecast
      min_b: !input min_backup
      max_b: !input max_backup
      cutoff: !input cutoff_hour

      backup_today: >
        {% set f = forecast_today %}
        {% if f < min_f %}{% set f = min_f %}{% elif f > max_f %}{% set f = max_f %}{% endif %}
        {{ [min_b, [((max_f - f) / (max_f - min_f) * (max_b - min_b) + min_b) | round(0), max_b] | min] | max }}

      backup_tomorrow: >
        {% set f = forecast_tomorrow %}
        {% if f < min_f %}{% set f = min_f %}{% elif f > max_f %}{% set f = max_f %}{% endif %}
        {{ [min_b, [((max_f - f) / (max_f - min_f) * (max_b - min_b) + min_b) | round(0), max_b] | min] | max }}

      backup_d2: >
        {% set f = forecast_d2 %}
        {% if f < min_f %}{% set f = min_f %}{% elif f > max_f %}{% set f = max_f %}{% endif %}
        {{ [min_b, [((max_f - f) / (max_f - min_f) * (max_b - min_b) + min_b) | round(0), max_b] | min] | max }}

      backup_d3: >
        {% set f = forecast_d3 %}
        {% if f < min_f %}{% set f = min_f %}{% elif f > max_f %}{% set f = max_f %}{% endif %}
        {{ [min_b, [((max_f - f) / (max_f - min_f) * (max_b - min_b) + min_b) | round(0), max_b] | min] | max }}

      backup_total: >
        {% if now_hour < cutoff %}
          {% set total = (backup_today + backup_tomorrow + backup_d2 + backup_d3) / 4 %}
        {% else %}
          {% set total = (backup_tomorrow + backup_d2 + backup_d3) / 3 %}
        {% endif %}
        {{ [min_b, [total | round(0), max_b] | min] | max }}

      current_value: "{{ states(!input backup_target) | int(0) }}"

  - choose:
      - conditions:
          - condition: template
            value_template: "{{ backup_total != current_value }}"
        sequence:
          - service: number.set_value
            target:
              entity_id: !input backup_target
            data:
              value: "{{ backup_total }}"
          - service: !input notify_service
            data:
              title: "🔋 Batterie-Backup angepasst"
              message: >
                Backup Power wurde auf {{ backup_total }} % gesetzt.
                {% if now_hour < cutoff %}
                  Heute: {{ forecast_today }} kWh,
                {% endif %}
                Morgen: {{ forecast_tomorrow }} kWh, Übermorgen: {{ forecast_d2 }} kWh,
                In 3 Tagen: {{ forecast_d3 }} kWh.
mode: single
